DBMS:
sqlite

Error:
ambiguous column name: cohort_definition_id

SQL:
SELECT
  CAST('Eunomia' as TEXT) as database_id,
  cohort_definition_id AS cohort_id,
  COUNT(*) AS cohort_entries,
  COUNT(DISTINCT subject_id) AS cohort_subjects
FROM main.strategus_test,strategus_test_inclusion,strategus_test_inclusion_result,strategus_test_inclusion_stats,strategus_test_summary_stats,strategus_test_censor_stats 
GROUP BY cohort_definition_id;


R version:
R version 4.1.3 (2022-03-10)

Platform:
x86_64-w64-mingw32

Attached base packages:
- stats
- graphics
- grDevices
- utils
- datasets
- methods
- base

Other attached packages: